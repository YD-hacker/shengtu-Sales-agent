# 盛途 Sales Agent 产品优化共创研讨报告

> 生成日期：2026-05-12
> 基于代码版本：server_backup/ (commit 81a6678)

---

## 一、核心优化共创结论摘要

当前系统在工程完整性上达到生产级水准，但在**销售场景深度适配**、**对话自然度与合规的精细平衡**、**数据闭环驱动优化**三个维度存在明显天花板。最大问题集中：七步SOP过于线性，无法处理真实销售中"问价-折返-再问"的非线性行为；信任门禁的硬分数卡口缺乏情景弹性；合规红线存在"高薪""大厂"等隐性承诺盲区且兜底话术未做挽留设计；豆包单点依赖无跨厂商灾备。核心建议：SOP引入"弹性路径"模型而非线性状态机，信任引擎增加情景权重和用户感知缓冲，合规体系增补三层隐性承诺检测，模型层增加OpenAI/Claude灾备路由。

---

## 二、分模块优化方案详述

### 环节1：SOP与转化漏斗优化

#### a) 七步SOP vs 真实销售行为

**当前代码位置**：`state_machine.py:150-250` `get_next_state()`
**当前问题**：严格线性状态机 `icebreak→qualify→match_campus→show_fee→invite→report_info→completed` 不匹配真实销售的非线性行为：

- 学员在icebreak就问费用（`fee_intent`），当前仅回复"费用的事你放心"继续收集资质（`agent_core.py:1388-1421`）——流失急着比价的线索
- 看完费用后退回犹豫（`reject_qualify→intent=objection_*→停留在reject_qualify`），无法回退到qualify重新协商
- 无"并行轨道"——A类学员和C类学员走完全相同的步骤序列

**优化方案**：

| 措施 | 具体方案 | 代码影响范围 | 预期收益 | 复杂度 |
|------|----------|-------------|---------|--------|
| 多路径状态机 | `get_next_state()` 增加 `lead_grade` 参数，B级跳过match_campus直通show_fee | `state_machine.py` + `decision_engine.py` | 缩短B/C级线索50%对话轮次 | 中 |
| 费用预问弹性处理 | qualify阶段用户反复问价≥2次，提前进入mini版show_fee子流程（只给区间），自动回到qualify | `agent_core.py` L1388区段 | 减少因"不回答费用"导致的流失约15% | 中 |
| 异议回退路径 | show_fee被price objection时，增加回退match_campus推荐更便宜方案的路径 | `state_machine.py` + `decision_engine.py` | 挽回约10%价格敏感流失 | 低 |
| 从yaml读取路径模板 | `should_skip_steps` 条件改为配置驱动而非硬编码 | `decision_engine.py:12-53` | 运营可自主调优路径 | 低 |

#### b) 信任门禁过于机械

**当前代码位置**：`trust_engine.py:123-137` `can_advance_to()`
**当前问题**：硬阈值比较 `trust >= threshold`，无历史依赖、无场景弹性：

- trust=51今天可通过show_fee，明天衰减到49被拦住——体验断崖
- S级线索即便trust=45也应允许报价

**优化方案**：统一门禁入口，将 `decision_engine.py:125-149` 的 `get_dynamic_threshold()` 合并到 `trust_engine.can_advance_to()`，增加24小时通过缓存（hysteresis）。

#### c) 20轮硬上限

**当前代码位置**：`decision_engine.py:164-175`
**问题**：S级线索刚聊到show_fee，第20轮断然结束。

**优化**：轮次上限按线索等级动态调整——S级40轮，A级30轮，B/C级维持20轮。达上限前5轮自然植入"小结推进"话术。

#### d) 费用展示时机

**当前代码位置**：`agent_core.py:1906-1945`（match_campus快速通道）
**优化**：报价后自动衔接邀约催促，减少价格敏感用户犹豫窗口。

---

### 环节2：对话质量与体验优化

#### a) 模板直出与LLM生成风格割裂

**当前代码位置**：`agent_core.py:2117`（模板节点）vs `agent_core.py:2168`（LLM节点）
**问题**：模板通道正式一口价风格，LLM通道口语化带表情——用户感知"换人"。LLM改写通道强制 `temperature=0.1` 输出与原话相同（`agent_core.py:2269-2312`），浪费LLM能力。

**优化**：模板节点也走LLM但加载人设上下文，temperature=0.3，用KB核心信息+人设重新组织语言。所有输出统一过风格一致性检查。

#### b) 模糊确认降级误杀

**当前代码位置**：`intent_classifier.py:284-293`
**问题**："好的""行"在微信对话中是真实确认，降级为normal后状态机不推进，引发用户挫败感。

**优化**：短confirm改为置信度打折(×0.7)而非直接改意图，与LLM二次判断结果比较取优。增加"已确认字段计数"上下文辅助判断。

#### c) 辱骂检测精准度

**当前代码位置**：`intent_classifier.py:42-45`
**问题**：纯关键词匹配"操"在"操作"中被误杀。

**优化**：增加否定前缀检测，"操作""操作员"不触发。高风险词要求词组匹配。

#### d) 销售节奏调节

**优化**：整合 `decision_engine.py:245-274` 的 `conversation_pace` 到回复生成中，根据用户回应速度和情感状态动态调整推进力度：fast/normal/slow 三档。

---

### 环节3：合规红线深度学习与加固

⚠️ **合规专家拥有一票否决权**

#### a) 隐性承诺词库严重遗漏

**当前代码位置**：`compliance_checker.py:17-22`

**一票否决裁决：以下词语必须立即加入 GLOBAL_FORBIDDEN：**

```python
"高薪", "高工资", "轻松月入", "月入过万", "年薪",
"大厂", "名企", "头部企业", "上市公司直签",
"学完就", "毕业就进", "入职没问题",
"名额有限", "最后几个", "马上截止", "即将涨价",
"保底", "保底薪资", "保薪", "保就业率",
```

关于"名额有限"争议：运营认为转化率或下降20%，合规一票否决——2024年教育行业广告法执法案例中已认定为虚假促销。替代话术："这期班刚开课不久"。

#### b) "不是培训机构"豁免规则加固

**当前代码位置**：`compliance_checker.py:85-86`
**问题**：LLM可能生成"我不是培训机构，但我们学完能进大厂月薪15000起"——豁免前半句通过，后半句严重违规。

**加固**：确认外层和内层检查顺序，确保豁免仅对该违禁词，整条消息仍运行全局违禁词检查。

#### c) 合规拦截后的分级挽留话术

**当前代码位置**：`compliance_checker.py` 统一输出"有些话我不方便线上说，周末来校区我当面给你讲清楚。"

**优化方案**——分级兜底话术：

| 触发场景 | 替代话术 |
|---------|---------|
| 全局违禁词命中 | "这个问题涉及具体政策，我线上不方便说清楚。你来校区我给你看合同原文，上面写得明明白白。你平时周末方便还是工作日方便？" |
| 费用敏感词命中（早期阶段） | "费用的事我们明明白白说。你来校区我给你看详细的费用方案和工作保障条款，你亲自看最放心。这周末有空吗？" |
| 动态红线命中 | "你提到的这个情况，我建议你来校区看看实际的教学环境和学员反馈，比我在线上说更有说服力。" |

每条兜底话术末尾都带**邀约钩子**，把合规拦截转化为引导到店。

#### d) 数据合规（PIPL）

**当前代码位置**：`memory_manager.py`
**问题**：用户数据JSON明文存储（含手机号、姓名）。

**建议**：
- AES加密存储phone和name字段
- 增加 `delete_user_data(user_id)` API
- completed状态超90天自动清理

---

### 环节4：AI能力提升

#### a) 豆包能力边界与单点依赖

**当前代码位置**：`model_router.py`
**问题**：
1. 只有火山引擎一个后端，API故障时所有重试都会失败
2. 竞品对比意图误判为objection_institution而非layer2客观回答
3. "我考虑考虑"无法区分真犹豫 vs 礼貌拒绝

**优化**：

| 措施 | 具体 | 复杂度 |
|------|------|--------|
| 增加deepseek灾备路由 | `model_router.py` 增加 `FALLBACK_PROVIDER` | 中 |
| 竞品对比专用意图 | `intent_classifier.py` 增加 `competition_compare` | 低 |
| 考虑vs拒绝深度区分 | 对"我考虑考虑"增加LLM二次判断 | 低 |

#### b) 向量记忆检索质量

**当前代码位置**：`memory_vector.py`
**问题**：384维 `paraphrase-multilingual-MiniLM-L12-v2` 在中文语义区分度不够；整轮对话作为chunk太粗。

**优化**：
- 升级到 `BAAI/bge-m3`（1024维）或 `shibing624/text2vec-base-chinese`
- 按意图切割chunk
- 增加时间衰减权重（3天前0.8，7天前0.5）

#### c) 工具调用触发优化

**当前代码位置**：`agent_core.py:639-732`
**问题**：`len(msg) > 10` 限制过滤了短消息工具触发。

**优化**：移除改为 `tokens > 5`，增加工具调用失败时的人性化降级。

#### d) 熔断降级三级方案

**当前代码位置**：`model_router.py` + `agent_core.py`
**问题**：熔断时LLM_NODES（icebreak/qualify）用固定模板，所有用户回复完全相同。

**建议**：三级降级——LLM（正常）→ 本地Qwen2.5-7B（处理非关键节点）→ 模板（最后兜底）。

---

### 环节5：数据驱动的持续优化

#### a) 线索评分模型未经验证

**当前代码位置**：`lead_scorer.py:119-126`
**问题**：profile(0-40)+behavior(0-30)+intent(0-30) 权重凭经验，未用真实转化数据校准。

**优化**：在 `conversation_analytics.py` 增加 `lead_score_calibration` 事件，关联最终转化结果做回归分析修正权重。

#### b) A/B实验指标需升级

**当前代码位置**：`experiment_manager.py`
**问题**：指标停留在"模板曝光数"等虚荣指标，缺少到店率、报备完成率等商业指标。

**优化**：与 `channel_pusher.py` 的报备推送联动，`push_report_info` 成功时自动记录 `conversion`。

#### c) 看板无法定位流失节点

**当前代码位置**：`analytics_dashboard.py`
**优化**：增加"阶段间流失率"和"流失前最后意图"分布。

#### d) 画像特征置信度

**当前代码位置**：`user_profiler.py`
**问题**：决策风格等推断仅靠3轮对话2个信号，样本量不足。

**优化**：为每个画像维度增加置信度字段，在 `low_confidence` 时不使用该特征。

---

### 环节6：极端情况与容错

#### a) 法律风险场景处理 **

**当前代码位置**：`intent_classifier.py` — 缺少 `legal_threat` 意图
**风险**："我报警了""我在录屏""你是AI吧我要投诉" → 被归类为normal → AI继续推销 → 执法视角下加重处罚。

**建议**：
1. 增加 `legal_threat` 意图（报警/投诉/举报/12315/录屏/证据/起诉），优先级120
2. 触发后 `state["_legal_threat_mode"] = True`
3. 回复切换为"你的反馈我已记录，我们会通过官方渠道联系你处理。"
4. 触发企微告警推送

#### b) 消息乱序与异步回复冲突

**当前代码位置**：`wechat_work.py:297-301` + `memory_manager.py:29`
**问题**：
1. 用户连发3条 → 3个线程并行 → 回复乱序
2. `_state_lock = threading.Lock()` 是进程内锁，Gunicorn多worker下无效

**优先修复**：
1. 用户级别消息队列化
2. 使用 `filelock` 实现跨进程文件锁
3. 或Gunicorn改为 `workers=1, threads=8`

#### c) 沉默用户回归信任重置

**当前代码位置**：`trust_engine.py:164-179` + `agent_core.py:973`
**问题**：30天未活跃用户回归时trust从50衰减到0，对话无法推进。但主动回归=有兴趣。

**建议**：沉默超14天后主动发消息，信任重置为 `min(50, decayed_value)`。

#### d) 挽回引擎时序冲突

**当前代码位置**：`recovery_engine.py:188-238`
**问题**：挽回任务执行前用户已主动回复，仍会发送挽回消息——"正聊着，发个挽回消息"。

**修复**：在 `record_active()` 中增加 `_cancel_pending_recovery(user_id)`。

---

## 三、优化优先级矩阵

按 **效果（商业价值提升）** × **紧急度（风险/损失）** 排序：

| 优先级 | 优化项 | 效果 | 紧急度 | 复杂度 | 归属模块 |
|--------|--------|------|--------|--------|---------|
| **P0** | 合规词库增补（高薪/大厂/名额有限等） | 中 | 极高（法律风险） | 低 | compliance_checker.py |
| **P0** | 法律风险场景处理（报警/投诉/录屏） | 中 | 极高（法律风险） | 中 | intent_classifier + agent_core |
| **P0** | 多worker状态竞争修复（文件锁/单worker） | 高 | 高（数据一致性） | 中 | memory_manager.py |
| **P1** | 三级兜底话术（合规拦截+邀约钩子） | 高 | 高（流失率） | 低 | compliance_checker.py |
| **P1** | 线索评分模型校准（真实数据回归） | 高 | 中 | 中 | lead_scorer + analytics |
| **P1** | 多路径状态机（等级差异化路径） | 高 | 中 | 中 | state_machine + decision_engine |
| **P1** | 信任门禁hysteresis（24小时通过缓存） | 中 | 中 | 低 | trust_engine.py |
| **P1** | 轮次上限按线索等级动态调整 | 中 | 中 | 低 | decision_engine.py |
| **P2** | 数据看板增加阶段间流失率 | 中 | 低 | 低 | analytics_dashboard.py |
| **P2** | 增加OpenAI/Claude灾备路由 | 中 | 中 | 中 | model_router.py |
| **P2** | 向量记忆升级到bge-m3+意图级chunk | 中 | 低 | 高 | memory_vector.py |
| **P2** | 熔断降级增加本地小模型层 | 低 | 中 | 高 | model_router.py |
| **P2** | 沉默用户回归信任重置 | 中 | 低 | 低 | trust_engine + agent_core |
| **P3** | 用户数据加密存储+删除API | 低 | 中（PIPL合规） | 中 | memory_manager.py |
| **P3** | A/B实验指标扩展到店率 | 中 | 低 | 中 | experiment_manager + channel_pusher |
| **P3** | 挽回引擎取消机制（用户已回来时） | 低 | 低 | 低 | recovery_engine + memory_manager |
| **P3** | 费用预问弹性处理子流程 | 中 | 低 | 中 | agent_core + state_machine |

---

## 四、争议问题裁决记录

| 争议 | 双方案由 | 终判结论 | 裁决人 |
|------|---------|---------|--------|
| "名额有限"是否应加入违禁词 | 合规：虚假紧迫感违法；运营：转化率下降20% | **加入违禁词，替换为"这期班刚开课不久"** | 合规专家一票否决 |
| 短confirm降级是否保留 | NLP：保留可防自嗨推进；产品：误杀真实确认 | **保留但改为置信度打折(×0.7)而非直接改意图**,由LLM二次判断 | 对话式AI专家 |
| 是否要升级faiss索引重建 | 运维：重建耗时；NLP：检索准确性更重要 | **仅在clear_user_memories时触发重建**,运行时增量添加即可 | NLP专家 |
| 多路径状态机是否一次性上线 | 运营：全量效果好；产品：渐变灰度更安全 | **A/B实验控制**,先开放B/C级的短路径实验 | 主持人判定 |

---

## 五、下一版本迭代建议

### V3.1 建议功能清单（按P0+P1优先级）

**阶段1：合规加固与稳定性（1-2周）**
1. 合规词库增补 + 三层隐性承诺检测
2. 法律风险场景处理（legal_threat意图+合规保护模式）
3. 多worker状态竞争修复（文件锁或gunicorn单worker）

**阶段2：转化率优化（2-3周）**
4. 三级兜底话术（合规拦截+邀约钩子，每个场景3个版本）
5. 多路径状态机（从config.yaml读取路径模板）
6. 信任门禁hysteresis（24小时通过缓存）
7. 轮次上限按线索等级动态调整

**阶段3：数据与AI能力（3-4周）**
8. 线索评分模型校准（转化数据回写+回归分析）
9. 数据看板增加阶段间流失率+流失前意图分布
10. 向量记忆升级（bge-m3 + 意图级chunk切割）

**工作量预估**：全职1名Python后端 + 0.5名运维约 **5-7周**可完成所有P0+P1项。P2/P3项视情况选择性推进。

---

*本报告基于盛途Sales Agent代码库（server_backup/）进行代码审查后生成，所有优化建议均指向具体文件及行号。*
